# Головоломка: PRIMARY SOURCES OR IT DIDN’T HAPPEN
Напишите скрипт на Python, который добывает только из первоисточников пять фактов, преобразует их и собирает финальный флаг такого формата:

```
FLAG{<voyager_date>-<rfc1149_date>-<brain_codepoint>-<btc_genesis_date>-<kr2_isbn10>}
```

1. <voyager_date> — дата запуска Voyager 1 в формате YYYYMMDD.
Требование: возьмите её с официального источника NASA/JPL, а не с энциклопедий и агрегаторов.
2. <rfc1149_date> — дата публикации RFC 1149 в формате YYYYMMDD.
Требование: возьмите дату со страницы IETF Datatracker / RFC Editor.
3. <brain_codepoint> — шестнадцатеричный кодпоинт эмодзи «🧠» БЕЗ префикса U+ (только заглавные hex-цифры).
Требование: возьмите кодпоинт из официальных материалов Unicode (стандарт/чарты/документы UTS/UAX), а не со сторонних сайтов.
4. <btc_genesis_date> — дата генезис-блока Bitcoin в формате YYYYMMDD.
Требование: подтвердите дату хотя бы одним первоисточником (оригинальный клиент/архив листинга/официальные репозитории или публикации создателя/ранних участников, а не «какой-то блог»).
5. <kr2_isbn10> — ISBN-10 второго издания “The C Programming Language” (Kernighan & Ritchie) без дефисов.
Требование: используйте официальный каталог издателя или библиотечный первоисточник (например, Library of Congress), а не книжные магазины.

Соберите строку флага и посчитай её SHA-256. Если всё сделал корректно, хеш совпадёт с эталоном ниже.
```
d311f26ea1a995af669a62758ad5e0ce2583331059fbfc5c04cc84b2d41f4aed
```

# Запуск 

```bash
uv venv
uv sync
uv run python -m src.main
```